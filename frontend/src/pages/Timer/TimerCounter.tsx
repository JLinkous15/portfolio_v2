import { Typography } from '@mui/material'

type TimerCounterProps = {
  timer: string
  setTimer: React.Dispatch<React.SetStateAction<string>>
}

export const TimerCounter = ({ timer, setTimer }: TimerCounterProps) => {
  const parseTime = () => {}
  return <Typography></Typography>
}
